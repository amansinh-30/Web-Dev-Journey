<%- include('partials/head') %>

    <%- include('partials/navbar') %>

        <div class="container">
            <h2>Top Car Companies in The World</h2>

            <!-- Create a loop to get Array from JS -->
            <ul>
                <% for(let car of cars){ %>
                    <li>
                        <%= car %>
                    </li>
                    <% } %>
            </ul>
        </div>

        <%- include('partials/footer') %>